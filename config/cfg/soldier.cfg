// Soldier configuration -*- mode: conf-javaprop -*-

// Load defaults
exec config

// Viewmodel should be off for the primary weapon, but on for secondary and melee.
alias primary   "r_drawviewmodel 0; slot1"
alias secondary "slot2; r_drawviewmodel 1"
alias melee     "slot3; r_drawviewmodel 1"
